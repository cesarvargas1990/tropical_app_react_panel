server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Opcional: bloquear acceso a archivos sensibles
  location ~ /\.(?!well-known).* {
    deny all;
  }
}
